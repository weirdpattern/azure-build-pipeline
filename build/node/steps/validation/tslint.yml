parameters:
  # overrides
  debug: $(debug)

  # step specifics
  glob: "."
  extra_arguments: ""

steps:
  - template: lint.yml
    parameters:
      displayName: 'TSLint linting'
      linter: "tslint"
      glob: "${{ parameters.glob }}"
      extra_arguments: "${{ parameters.extra_arguments}}"
